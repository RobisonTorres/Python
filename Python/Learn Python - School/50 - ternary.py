print('Working with ternary operators.')
print()

# Ternary operators also known as conditional expressions are operators\
# that evaluate something based on a condition being true or false.

# It simply allows to test a condition in a single line\
# replacing the multiline if-else making the code compact.

y = 6
x = 1 if y > 6 else 45
print(y)

a, b = 10, 20

# Copy value of a in min if a < b else copy b
min = a if a < b else b
